# Generated by ReXGlue v0.10.0.68-dev.g8dcc19f on 2026-09-15 05:45:30 UTC.

[project]
name = "geometrywars"
sdk_version = "0.10.0"
game_root = "assets"

[entrypoint]
file_path = "assets/default.xex"
out_directory_path = "generated/default"
includes = []

# Keep the PowerPC non-volatile registers in C++ locals rather than in the
# guest context. Two effects, both measured on SoulCalibur II: the
# __savegprlr_N/__restgprlr_N helper calls that every prologue and epilogue
# makes disappear entirely - they were 9% of all profile samples - and the
# compiler can then hold those registers in host registers instead of
# reloading them from the context on each use.
#
# Verified rendering-correct on SoulCalibur II HD and Banjo-Kazooie. Turn it
# off for a title that misbehaves in a way that looks like stale register
# state, since it does change what the generated code does between calls.
non_volatile_as_local = true

# Without these the longjmp is emitted as an ORDINARY CALL: the helper loads the
# saved registers into the context and returns normally, so control never
# unwinds and the caller carries on with non-volatile registers restored from a
# stale jmp_buf. It surfaces as an unrelated function calling a pointer it never
# computed - here sub_8205A580 calling 0x40008A60, which is data.
# Identified by their register sets: 0x82099600 restores f14-f31, r13-r31, LR,
# CR and r1 from a buffer and returns; 0x82099830 saves the same fields at the
# same offsets.
longjmp_address = 0x82099600
setjmp_address = 0x82099830
