# Choplifter HD cookbook

State: not working; native diagnostic candidate only.

## Required gates

1. `python3 scripts/test_recipe.py --owned --generated` must preserve exact
   title identity, import classification and the known unresolved-call set.
2. A fresh current-ReXGlue build must link and pass the central review.
3. Capture the first failing native route before attempting a runtime fix.
4. Add a bounded integration regression for that failure and take generated
   direct-call closure to zero.
5. Only a user-validated gameplay, graphics, audio, input, persistence and
   clean-close matrix may change this repository from `not-working`.

A successful link is not evidence that Choplifter works.
