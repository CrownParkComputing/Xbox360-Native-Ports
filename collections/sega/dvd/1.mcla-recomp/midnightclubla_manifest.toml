# Generated by ReXGlue v0.10.0.17-dev.g20e678a on 2026-09-13 03:20:09 UTC.

[project]
name = "midnightclubla"
sdk_version = "0.10.0"
game_root = "assets"

[entrypoint]
file_path = "assets/default.xex"
out_directory_path = "generated/default"
includes = []
# Earned back after the port is known-good (Phase 0 profile: the PPC save/
# restore helpers were ~12% of CPU on MCLA). Verify frames still render.
non_volatile_as_local = true
