# Generated by ReXGlue v0.10.0.2-dev.gc94f5eb on 2026-09-03 18:33:17 UTC.

[project]
name = "ridgeracerunbounded"
sdk_version = "0.10.0"
game_root = "assets"

[entrypoint]
file_path = "assets/default.xex"
out_directory_path = "generated/default"
includes = []
non_volatile_as_local = true
# Found by tools/find_setjmp.py. Undeclared, the longjmp is emitted as an
# ORDINARY CALL - control never unwinds and the caller continues with
# non-volatile registers restored from a stale jmp_buf, which surfaces far from
# the cause as a wrong pointer or a wrong computed value.
longjmp_address = 0x82B8F890
setjmp_address = 0x82B8F5C0

# A two-instruction thunk (lwz r3,0(r3); b RtlEnterCriticalSection) that sits
# past a gap the sweep does not enter. Everything else that once needed
# declaring here is now found generically.
[entrypoint.functions]
0x82913650 = { name = "sub_82913650", size = 8 }
0x82913658 = { name = "sub_82913658", size = 8 }
