# Generated by ReXGlue v0.10.0.2-dev.gc94f5eb on 2026-09-12 16:16:38 UTC.

[project]
name = "soulcalibur4"
sdk_version = "0.10.0"
game_root = "assets"

[entrypoint]
file_path = "assets/default.xex"
out_directory_path = "generated/default"
includes = []
# Found by tools/find_setjmp.py. Undeclared, the longjmp is emitted as an
# ORDINARY CALL - control never unwinds and the caller continues with
# non-volatile registers restored from a stale jmp_buf, which surfaces far from
# the cause as a wrong pointer or a wrong computed value.
longjmp_address = 0x825E22F0
setjmp_address = 0x825E2610

# Found by tools/port_check.py: sub_825BDAA8 calls 0x825BDA08, which the scanner
# never emitted. It builds, then kills the title the first time that path runs.
[entrypoint.functions]
0x825BDA08 = {}
